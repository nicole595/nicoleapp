import { StyleSheet } from "react-native/types";

export const styles = StyleSheet.create({
    title: {
        fontSize: 40,
        marginTop: 20,
        margin: 10,
        textAlign: "center",
    },
})